.\" "Generated by swift-argument-parser"
.Dd September 23, 2026
.Dt SHARE 1
.Os
.Sh NAME
.Nm share
.Nd "Share files from your Mac terminal via AirDrop, Mail, Messages, Shortcuts, or a local link."
.Sh SYNOPSIS
.Nm
.Oo
.Ar subcommand
.Oc
.Op Fl -version
.Op Fl -help
.Sh DESCRIPTION
Smart routing: with no subcommand, the arguments decide the destination.

  share                         AirDrop the current directory (zipped)
  share ./build.zip README.md   AirDrop those files
  share rey@example.com         Email the current directory as a draft
  share +14375550100 "hi"       Compose a Message
  share @rey ./report.pdf       Use an alias; groups fan out to everyone
  share airdrop|email|msg ...   Explicit destination with every option

Setup:    share init, share alias rey rey@example.com, share config set smart true
Inspect:  share preview ., share doctor, share history
Repeat:   share again

Safety defaults: email and messages create drafts unless --send is given,
nothing is uploaded anywhere, and folders are scanned for secrets first.
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.It Em "airdrop, ad, drop"
Share via AirDrop (opens the native device picker).
.Pp
Directories are zipped first. Several directories, or directories mixed with files, become one bundle. Use --no-zip to send a folder as-is, or --smart to leave out dependencies, build output and secrets.
.Bl -tag -width 6n
.It Ar items...
Files, directories, or URLs. Defaults to the current directory.
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl n , -name Ar name
Archive name, without the .zip extension.
.It Fl -smart
Exclude VCS metadata, dependencies, build output and secrets (honors .gitignore and .shareignore).
.It Fl -no-zip
Share directories as-is instead of zipping them.
.It Fl -exclude Ar exclude...
Extra ignore pattern, gitignore style. Repeatable.
.It Fl -clipboard
Share the clipboard contents (text or image) instead of files.
.It Fl -timeout Ar timeout
Seconds to wait for the AirDrop panel (default from config, else 300).
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em "email, mail, em"
Create a Mail.app draft (or send) with the items attached.
.Pp
URLs and text become part of the body; files and zipped directories are attached. Piped stdin is appended to the body. Use --send to send without reviewing.
.Bl -tag -width 6n
.It Ar to
Recipient: email address, @alias, or comma-separated list.
.It Ar items...
Files, directories, URLs, or - for stdin. Defaults to the current directory.
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl n , -name Ar name
Archive name, without the .zip extension.
.It Fl -smart
Exclude VCS metadata, dependencies, build output and secrets (honors .gitignore and .shareignore).
.It Fl -no-zip
Share directories as-is instead of zipping them.
.It Fl -exclude Ar exclude...
Extra ignore pattern, gitignore style. Repeatable.
.It Fl s , -subject Ar subject
Subject line. Default: "Shared: <repo> (<branch>)" or the configured template.
.It Fl b , -body Ar body
Body text. Use - to read it from stdin.
.It Fl -body-file Ar body-file
Read the body from a file.
.It Fl -from Ar from
Sender address (default: config 'from').
.It Fl -cc Ar cc
CC recipients, comma-separated.
.It Fl -bcc Ar bcc
BCC recipients, comma-separated.
.It Fl -send
Send immediately instead of opening a draft.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em "messages, message, msg, im, sms"
Share via Messages (iMessage or SMS).
.Pp
Arguments that are not existing files become the message text. Without --send, the conversation opens with the text pre-filled and any files on the clipboard (V to attach). With --send, text and files go out immediately.
.Bl -tag -width 6n
.It Ar recipient
Recipient: phone number, iMessage email, or @alias.
.It Ar items...
Text, files, directories, or URLs.
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl n , -name Ar name
Archive name, without the .zip extension.
.It Fl -smart
Exclude VCS metadata, dependencies, build output and secrets (honors .gitignore and .shareignore).
.It Fl -no-zip
Share directories as-is instead of zipping them.
.It Fl -exclude Ar exclude...
Extra ignore pattern, gitignore style. Repeatable.
.It Fl t , -text Ar text
Message text (alternative to positional words). Use - for stdin.
.It Fl -send
Send immediately instead of opening a draft.
.It Fl -sms
Use the SMS account (iPhone relay) instead of iMessage.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em "shortcut, sc"
Run a macOS Shortcut with the items as input.
.Pp
Files are passed with the shortcut's file input; URLs and text are piped on stdin.
.Bl -tag -width 6n
.It Ar shortcut-name
Name of the Shortcut to run.
.It Ar items...
Files, directories, URLs or text. Defaults to the current directory.
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl n , -name Ar name
Archive name, without the .zip extension.
.It Fl -smart
Exclude VCS metadata, dependencies, build output and secrets (honors .gitignore and .shareignore).
.It Fl -no-zip
Share directories as-is instead of zipping them.
.It Fl -exclude Ar exclude...
Extra ignore pattern, gitignore style. Repeatable.
.It Fl l , -list
List available Shortcuts.
.It Fl o , -output-path Ar output-path
Write the Shortcut's output to this path.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em "serve, link, http"
Serve a file on your local network with a scannable QR code (no AirDrop needed).
.Pp
Starts a small HTTP server on this Mac and prints a link (and QR code) that any phone, tablet or laptop on the same Wi-Fi can open to download the file. Directories and multiple items are zipped first. The link uses a random token, nothing is uploaded anywhere, and the server stops on Ctrl-C, after --timeout, or after the first download with --once.
.Bl -tag -width 6n
.It Ar items...
File or directory to serve. Defaults to the current directory.
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl n , -name Ar name
Archive name, without the .zip extension.
.It Fl -smart
Exclude VCS metadata, dependencies, build output and secrets (honors .gitignore and .shareignore).
.It Fl -no-zip
Share directories as-is instead of zipping them.
.It Fl -exclude Ar exclude...
Extra ignore pattern, gitignore style. Repeatable.
.It Fl p , -port Ar port
Port to listen on (default: config 'servePort', else a random free port).
.It Fl -once
Stop after the first completed download.
.It Fl -timeout Ar timeout
Stop after this many seconds (default 600; 0 = until Ctrl-C).
.It Fl -no-qr
Do not print the QR code.
.It Fl -no-copy
Do not copy the link to the clipboard.
.It Fl -host Ar host
Advertise this host/IP in the link instead of the detected one.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em zip
Package files and folders into a zip archive.
.Pp
Without --output the archive goes to the scratch directory and its path is copied to the clipboard. --output may be a file path or an existing directory.
.Bl -tag -width 6n
.It Ar items...
Files or directories to package. Defaults to the current directory.
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl n , -name Ar name
Archive name, without the .zip extension.
.It Fl o , -output-path Ar output-path
Output file path, or a directory to place the archive in.
.It Fl -smart
Exclude VCS metadata, dependencies, build output and secrets.
.It Fl -exclude Ar exclude...
Extra ignore pattern, gitignore style. Repeatable.
.It Fl -force
Overwrite the output file if it exists.
.It Fl -no-copy
Do not copy the archive path to the clipboard.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em "copy, cp, clip"
Copy a path, URL, file contents, or a fresh zip to the clipboard.
.Bl -tag -width 6n
.It Ar items...
File, directory, or URL. Defaults to the current directory.
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl -zip
Zip first, then copy the archive path.
.It Fl -file-url
Copy as a file:// URL.
.It Fl -file
Copy the file itself (paste into Finder, Mail, Slack) instead of its path.
.It Fl -contents
Copy the file's text contents.
.It Fl -smart
Exclude VCS metadata, dependencies, build output and secrets when zipping.
.It Fl n , -name Ar name
Archive name when zipping.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em "text, txt"
Share a snippet of text: from arguments, stdin, or the clipboard.
.Pp
With --to it goes out via email or Messages; otherwise it is copied to the clipboard.
.Bl -tag -width 6n
.It Ar words...
Text to share. If omitted, reads stdin (or the clipboard with --clipboard).
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl t , -to Ar to
Recipient: email, phone, or @alias.
.It Fl s , -subject Ar subject
Email subject.
.It Fl -clipboard
Read the text from the clipboard.
.It Fl -send
Send immediately instead of opening a draft.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em diff
Share the current git diff by email, Messages, or AirDrop.
.Pp
Short diffs are sent inline; long ones (or --attach) are sent as a .patch file.
Examples:
  share diff rey@example.com
  share diff @rey --staged
  share diff +14375550100 --range main..HEAD --send
  share diff airdrop
.Bl -tag -width 6n
.It Ar recipient
Recipient (email, phone, @alias) or 'airdrop'.
.It Ar paths...
Limit the diff to these paths.
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl -staged
Diff the staged changes only.
.It Fl -range Ar range
Git revision range, e.g. main..HEAD or HEAD~3.
.It Fl s , -subject Ar subject
Email subject.
.It Fl -attach
Always attach as a .patch file instead of inlining.
.It Fl -send
Send immediately instead of opening a draft.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em "batch, multi"
Share the same items with several recipients at once.
.Pp
Recipients are comma-separated and may mix emails, phone numbers and @aliases (including group aliases). Items are packaged once and reused for every recipient.
.Bl -tag -width 6n
.It Ar recipients
Comma-separated recipients: rey@a.com,+14375550100,@team
.It Ar items...
Files, directories, or URLs. Defaults to the current directory.
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl n , -name Ar name
Archive name, without the .zip extension.
.It Fl -smart
Exclude VCS metadata, dependencies, build output and secrets (honors .gitignore and .shareignore).
.It Fl -no-zip
Share directories as-is instead of zipping them.
.It Fl -exclude Ar exclude...
Extra ignore pattern, gitignore style. Repeatable.
.It Fl s , -subject Ar subject
Email subject.
.It Fl b , -body Ar body
Email body or message text.
.It Fl -send
Send immediately instead of opening drafts.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em qr
Show a QR code for text or a URL, right in the terminal.
.Pp
By default the code is printed in the terminal (scan it with your phone). Add --copy, --output or --open to get a PNG instead. When stdout is not a terminal, the PNG is copied to the clipboard.
.Bl -tag -width 6n
.It Ar content...
Text or URL to encode. Reads stdin when omitted.
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl o , -output-path Ar output-path
Save the QR code as a PNG at this path.
.It Fl -copy
Copy the PNG to the clipboard.
.It Fl -open
Open the PNG in Preview.
.It Fl p , -print
Print in the terminal (default when no other output is chosen).
.It Fl -scale Ar scale
Pixel size of each module in the PNG (default 10).
.It Fl -correction Ar correction
Error correction level: L, M, Q or H (default M).
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em "screenshot, ss, snap"
Capture a screenshot and share it.
.Pp
Without a recipient the image is copied to the clipboard. Pass an email, phone number, @alias, or 'airdrop' to send it directly. --selection and --window use the interactive picker.
.Bl -tag -width 6n
.It Ar recipient
Recipient (email, phone, @alias) or 'airdrop'. Omit to copy to the clipboard.
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl s , -selection
Capture a selection instead of the full screen.
.It Fl w , -window
Capture a single window.
.It Fl d , -delay Ar delay
Seconds to wait before capturing.
.It Fl -save Ar save
Also save the screenshot to this path.
.It Fl -keep
Keep the temporary screenshot file after sharing.
.It Fl -send
Send immediately instead of opening a draft.
.It Fl -subject Ar subject
Email subject.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em "open, reveal, finder"
Open items in their default app, or reveal them in Finder.
.Bl -tag -width 6n
.It Ar items...
Files, directories, or URLs. Defaults to the current directory.
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl r , -reveal
Reveal in Finder instead of opening.
.It Fl a , -app Ar app
Open with a specific application, e.g. --app 'Visual Studio Code'.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em "preview, ls, info"
Show what would be shared: sizes, project type, exclusions, and sensitive files.
.Bl -tag -width 6n
.It Ar items...
Files, directories, or URLs. Defaults to the current directory.
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl -smart
Show what --smart would exclude.
.It Fl -exclude Ar exclude...
Extra ignore pattern to evaluate. Repeatable.
.It Fl -all
List every excluded path instead of the first few.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em "again, last, redo"
Repeat the last share exactly as it was run.
.Bl -tag -width 6n
.It Fl -dry-run
Show what the last share was without repeating it.
.It Fl i , -index Ar index
Repeat the Nth most recent share instead of the last (1 = last).
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em "history, log"
Show recent shares.
.Bl -tag -width 6n
.It Fl c , -count Ar count
Number of entries to show.
.It Fl -all
Show every entry.
.It Fl -clear
Delete the history file.
.It Fl -json
Output JSON.
.It Fl y , -yes
Answer yes to confirmations.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em alias
Manage recipient aliases (@name  email, phone, or a comma-separated group).
.Pp
share alias                       list aliases
share alias rey rey@example.com   create or update
share alias team a@x.com,b@y.com  group alias: 'share @team .' fans out to everyone
share alias rey --remove          delete
.Bl -tag -width 6n
.It Ar name
Alias name (with or without @).
.It Ar value
Email, phone, or comma-separated group. Omit to show the current value.
.It Fl r , -remove
Remove the alias.
.It Fl -json
Output JSON.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em config
View or change settings.
.Pp
share config                     show the effective settings
share config set smart true      set a key in ~/.config/share/config.json
share config set from me@x.com --local   write to ./.share.json instead
share config unset smart
share config get from
share config path
share config edit                open the config file in $EDITOR
share config keys                list every key with a description
.Bl -tag -width 6n
.It Fl -json
Output JSON.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.It Em get
Print one setting.
.Bl -tag -width 6n
.It Ar key
Config key.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em set
Set one setting.
.Bl -tag -width 6n
.It Ar key
Config key.
.It Ar value
New value.
.It Fl -local
Write to ./.share.json instead of the global file.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em unset
Remove one setting.
.Bl -tag -width 6n
.It Ar key
Config key.
.It Fl -local
Edit ./.share.json instead of the global file.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em path
Print the config file locations.
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em edit
Open the config file in $EDITOR.
.Bl -tag -width 6n
.It Fl -local
Edit ./.share.json instead of the global file.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em keys
List every config key.
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.El
.It Em completions
Print or install shell completions (zsh, bash, fish).
.Bl -tag -width 6n
.It Ar shell
Shell: zsh, bash, or fish. Defaults to $SHELL.
.It Fl -install
Install into the shell's completion directory.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em init
Interactive first-run setup: aliases, defaults, and shell completions.
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em clean
Remove temporary archives, staged copies and screenshots.
.Pp
Every run already prunes scratch files older than 24 hours; this removes them on demand.
.Bl -tag -width 6n
.It Fl -all
Remove everything, regardless of age.
.It Fl -older-than Ar older-than
Remove entries older than this many hours (default 24).
.It Fl -dry-run
Show what would be removed.
.It Fl -json
Output JSON.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em doctor
Check that everything share needs is in place.
.Bl -tag -width 6n
.It Fl -json
Output JSON.
.It Fl -automation
Also test Mail and Messages automation (macOS may show permission prompts).
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em _default
.Bl -tag -width 6n
.It Ar remaining...
Recipient and/or items.
.It Fl -dry-run
Show what would happen without doing it.
.It Fl -json
Print the result as JSON on stdout.
.It Fl v , -verbose
Print extra detail.
.It Fl q , -quiet
Suppress status output. Errors are still printed.
.It Fl y , -yes
Answer yes to every confirmation prompt.
.It Fl -color
Force colored output.
.It Fl -no-color
Disable colored output.
.It Fl n , -name Ar name
Archive name, without the .zip extension.
.It Fl -smart
Exclude VCS metadata, dependencies, build output and secrets (honors .gitignore and .shareignore).
.It Fl -no-zip
Share directories as-is instead of zipping them.
.It Fl -exclude Ar exclude...
Extra ignore pattern, gitignore style. Repeatable.
.It Fl s , -subject Ar subject
Email subject.
.It Fl b , -body Ar body
Email body or message text.
.It Fl -from Ar from
Sender address for email.
.It Fl -cc Ar cc
CC recipients for email (comma-separated).
.It Fl -bcc Ar bcc
BCC recipients for email (comma-separated).
.It Fl -send
Send immediately instead of leaving a draft (email and messages).
.It Fl -sms
Use the SMS account instead of iMessage.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em help
Show subcommand help information.
.Bl -tag -width 6n
.It Ar subcommands...
.It Fl -version
Show the version.
.El
.El
.Sh "EXIT STATUS"
.Ex -std